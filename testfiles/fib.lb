
function fib(x) {
    // If more than 2, recursive definition
    if (x > 2)
        return fib(x - 1) + fib(x - 2);
    return 1;
}

assert(fib(10) == 55);
assert(fib(4) + fib(5) == fib(6));
assert(fib(3) > 1);